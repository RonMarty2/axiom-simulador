<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true">

        <!-- LauncherActivity de la librería androidbrowserhelper: no hace
             falta código Java/Kotlin propio, ella se encarga de abrir
             axiom-simulador.vercel.app dentro de una Trusted Web Activity. -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.LauncherActivity"
            android:exported="true">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://axiom-simulador.vercel.app/dashboard" />

            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/colorPrimaryDark" />

            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/backgroundColor" />

            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />

            <!-- Si el usuario no tiene Chrome (poco común), cae a una pestaña
                 normal en vez de romper la app. -->
            <meta-data
                android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="customtabs" />

            <meta-data
                android:name="asset_statements"
                android:resource="@string/asset_statements" />

            <meta-data
                android:name="android.support.customtabs.trusted.WEB_MANIFEST_URL"
                android:value="https://axiom-simulador.vercel.app/manifest.webmanifest" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Verificación de dominio: si assetlinks.json está bien
                 configurado (ver README), esto hace que los links a
                 axiom-simulador.vercel.app abran DENTRO de la app en vez del
                 navegador, y saca la barra de direcciones. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="axiom-simulador.vercel.app" />
            </intent-filter>
        </activity>

    </application>
</manifest>
